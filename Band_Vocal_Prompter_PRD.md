# 밴드 보컬용 프롬프터 웹 애플리케이션 PRD

## 1. 제품 개요 (Product Overview)
라이브 밴드 공연 시 보컬이 태블릿으로 가사를 타이밍에 맞춰 확인하는 HTML 기반 프롬프터 웹 애플리케이션. 노래방식 화려한 애니메이션을 배제하고, 수동 싱크 기록 + 라이브 드리프트에 견디는 실시간 보정에 집중해 무대 실용성을 극대화한다.

**핵심 설계 원칙**
- 공연 중 오디오를 재생하지 않는다. 프롬프터는 순수 타이머(시계)로 동작한다. 오디오는 편집 모드에서 싱크를 찍을 때만 쓰인다.
- 라이브 밴드는 곡 전체가 조금씩 빨라지거나 느려진다(누적 드리프트). 고정 오프셋 하나로 못 잡으므로, **"현재 줄로 재동기화"** 를 1차 안전장치로 둔다.
- 무대에서의 조작은 최소화하고, 오터치를 구조적으로 방지한다.

## 2. 타겟 사용자 및 사용 환경 (Target User & Environment)
- **사용자:** 라이브 공연을 진행하는 밴드 보컬 / 보조 오퍼레이터
- **사용 기기:** 태블릿 PC (아이패드, 갤럭시 탭 등), **가로(landscape) 기준**
- **사용 환경:** 악기 소리가 크고 조명이 어두운 라이브 공연장 무대 위. 오디오 출력 없이 시각 정보만 제공.

## 3. 핵심 기능 요구사항 (Key Functional Requirements)

### 3.1. 편집 모드 — 가사 입력 & 싱크 기록 (Edit Mode)
- **가사 입력:** 텍스트 붙여넣기(textarea). 줄바꿈 = 한 줄. **빈 줄은 '간주 대기' 줄(♪ 표시)로 유지** 해 전주·간주에서 화면이 비지 않게 한다.
- **싱크 기록:** 기준 음원(오디오 파일)을 업로드해 재생하면서, 각 줄이 시작되는 순간 **"찍기" 버튼(또는 스페이스바)** 으로 현재 재생시간을 그 줄의 `time`으로 저장한다. 저장 후 자동으로 다음 줄로 커서 이동.
- **수정:** 각 줄 옆에 기록된 시간을 표시하고, 특정 줄을 탭하면 그 줄부터 다시 기록한다.
- **BPM 입력:** 곡마다 BPM을 저장한다. 숫자 직접 입력 + **탭 템포**(화면을 여러 번 두드려 BPM 자동 산출) 보조.
- 파형 표시는 하지 않는다(미니멀). 재생/일시정지 + 현재시간 숫자로 충분.

### 3.2. 공연 뷰어 모드 (Performance Mode)

#### 3.2.1. 상태 흐름
```
세트리스트 목록 → [곡 탭] → 대기 화면(곡 제목 + "탭하여 시작")
→ [화면 탭] → BPM 카운트인(… 2 → 1 → GO) → 프롬프터 진행
→ 마지막 줄에서 정지(화면 유지) → [세트리스트로] → 목록
```

#### 3.2.2. 원탭 BPM 카운트인
- 드럼이 카운트를 세는 동안 오퍼레이터가 **다운비트 N박 전에 화면을 한 번 탭**한다(기본 N=2 → 드럼 카운트 "2"에 탭).
- 앱은 저장된 BPM으로 남은 박을 계산해 다운비트에 **타이머 0초**를 시작한다. 사람은 한 번만 누르며, 짧은 구간이라 BPM 오차의 영향은 무시할 수준.
- 카운트인 시각 표시: 화면 중앙 큰 숫자가 매 박 깜빡(… 2 → 1 → GO). 무음.
- `N`(탭 후 다운비트까지 박 수)은 곡마다 설정하며 기본값 2 (1/2/4 선택).

#### 3.2.3. 가사 출력
- **3줄 표시:** 이전 줄 · **현재 줄** · 다음 줄. 화려한 애니메이션 없음.
- 타이머 0초 기준, 편집 때 기록한 타임스탬프에 맞춰 현재 줄이 갱신된다(**전주·간주 포함 재현**).
- **강조:** 검은 배경. 현재 줄 = 노란색(가장 밝고 크게), 이전/다음 줄 = 회색(어둡고 작게).

#### 3.2.4. 오디오 무음 처리
- 공연 모드는 오디오를 재생하지 않는다. 프롬프터 시계는 순수 타이머(`performance.now()` 등)로 구동한다.

### 3.3. 실시간 싱크 보정 (Sync Adjustment)
진행 중 하단에 대형 버튼을 항상 노출한다. 역할을 분리한다.

- **재동기화(중앙):** 현재(노란) 줄의 시작 시각을 "지금 이 순간"으로 스냅한다. 큰 어긋남을 한 번에 교정하는 1차 수단.
- **일시정지/재개(중앙):** 타이머를 멈추고 이어서 재개한다.
- **`<` (왼쪽 끝):** 프롬프터를 **뒤로** — 가사가 더 늦게 뜨게. (가사가 밴드보다 앞설 때) 1회 ±1초.
- **`>` (오른쪽 끝):** 프롬프터를 **앞으로** — 가사가 더 빨리 뜨게. (밴드가 앞서고 가사가 뒤처질 때) 1회 ±1초.

큰 어긋남은 재동기화로, 잔여 미세 조정은 `<` / `>`(±1초)로 처리한다.

## 4. UI/UX 요구사항 (UI/UX Requirements)
- **오터치 방지 — 상태별 탭 구분:**
  - *대기 상태:* 화면 전체가 "탭하여 시작" 타겟(곡 아직 미시작이라 오발동 위험 없음).
  - *진행 상태:* 가사 영역은 반응하지 않는 죽은 영역. 하단 대형 버튼(`<` · 재동기화 · 일시정지 · `>`)만 반응.
- **태블릿 최적화:** 터치 타겟을 큼직하게. 가로 레이아웃 기준.
- **다크 테마:** 검은 배경 + 노란/흰 굵은 큰 글씨(고대비)로 눈부심 방지·가독성 확보.
- **화면 꺼짐 방지:** 공연 모드 진입 시 Wake Lock API로 화면 항상 켜짐 유지. 미지원 브라우저는 "자동잠금을 꺼 두세요" 안내로 폴백.
- **긴 줄 처리:** 화면보다 긴 가사는 자동 줄바꿈(wrap) + 화면 폭에 맞춰 폰트 자동 축소(하한선 있음). 텍스트 잘림 금지.
- **글자 크기:** 기본 크게 + 3단계(크게/더크게/최대) 조절.
- **미니멀 디자인:** 불필요한 테두리·장식 제거. '가사'와 '보정 버튼'에만 집중.

## 5. 데이터 모델 & 저장 (Data & Persistence)
- **오디오는 저장하지 않는다.** 공연 모드가 음원을 쓰지 않으므로 JSON에는 가사·타임스탬프·BPM·카운트인 설정만 담는다(수 KB, 세트리스트 통째 공유 용이). 음원은 재편집 시에만 다시 불러온다.
- **세트리스트 단위:** 여러 곡을 한 파일로 관리, 무대에선 곡 이름만 탭해 진입.
- **저장 방식:**
  - localStorage 자동저장 — 앱을 껐다 켜도 세트리스트 유지(무대 안전).
  - JSON 내보내기/불러오기 — 백업·기기 간 이동·공유.
- **골격:**
```json
{
  "settings": {},
  "songs": [
    {
      "title": "곡 제목",
      "bpm": 120,
      "tapBeatsBeforeDownbeat": 2,
      "lines": [
        { "text": "가사 한 줄", "time": 8.4 },
        { "interlude": true, "time": 20.1 }
      ]
    }
  ]
}
```

## 6. 기술 스택 (Technical Stack)
- 단일 파일로 오프라인 구동 가능하도록 **HTML5, CSS3, Vanilla JavaScript** 기반(별도 서버·DB 불필요).
- 사용 API: Wake Lock(화면 유지), Web Audio/`<audio>`(편집 모드 재생), localStorage(자동저장), File API(JSON·오디오 입출력).

## 7. 범위 밖 (Out of Scope, v1)
- 공연 모드에서의 오디오 출력·재생.
- 탭 템포로 얻은 실제 BPM을 타임라인 전체에 반영하는 **템포 스케일링**(다운비트 정렬만 하고, 잔여 드리프트는 재동기화/`<``>`로 처리). 필요 시 후속 버전에서 검토.
- 노래방식 글자 단위 애니메이션.
